🛒 Rozetka Clone
Опис проєкту

Rozetka Clone — це навчальний проєкт, створений з метою повторити основний функціонал сайту інтернет магазину
.
Проєкт реалізований на ASP.NET Core (бекенд) та React TypeScript (фронтенд) з метою практичного вивчення технологій розробки сучасних веб-додатків.

⚙️ Технології  
Backend (API)

ASP.NET Core 8 / 9

Entity Framework Core

PostgreSQL (або SQL Server)

AutoMapper

JWT Authentication

Swagger / OpenAPI

Dependency Injection

Frontend

React 18 + TypeScript

Vite або Create React App

Redux Toolkit / RTK Query

React Router v6

Ant Design або Material UI

Axios / fetch API

---

## Міграції БД (Entity Framework)

Після змін у сутностях виконай міграцію:

```bash
cd backend/src/CloneRozetka.Api
dotnet ef database update --project ../CloneRozetka.Infrastructure/CloneRozetka.Infrastructure.csproj --startup-project .
```

Або в Package Manager Console (Visual Studio):

```powershell
Update-Database
```

---

## Перевірка флоу: кошик та замовлення

1. **Логін** — увійдіть в акаунт (кошик прив’язаний до користувача).
2. **Кошик** — додайте товар з каталогу або картки товару (кнопка «Купити»). Перейдіть у «Кошик» — мають відображатися позиції з сервера.
3. **Редагування** — на сторінці кошика змініть кількість (+/-) або видаліть позицію; зміни зберігаються на бекенді.
4. **Оформлення** — натисніть «Оформити замовлення». Заповніть контакти та виберіть місто/відділення Нової Пошти.
5. **Підтвердження** — натисніть «Підтвердити замовлення». Створюється замовлення з поточного кошика в БД, кошик очищається.
6. **Список замовлень** — у «Мої замовлення» перевірте нове замовлення; у деталях має бути snapshot товарів (назва, ціна, кількість).
